df_name <- data.frame( name = c("Lee", "Park", "So", "Kim"),
                       age = c(55, 47, 35, 26),
                       height = c(170, 162, 166, 159))
df_name

mean(df_name$age)
mean(df_name$height)
